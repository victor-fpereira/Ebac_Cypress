{
  "dependencies": {
    "cypress-dotenv": "^2.0.0",
    "dotenv": "^16.3.1",
    "faker": "^6.6.6",
    "mochawesome": "^7.1.3",
    "mochawesome-report-generator": "^6.2.0"
  },
  "devDependencies": {
    "@faker-js/faker": "^7.6.0"
  },
  "scripts": {
    "test": "npx cypress run --reporter mochawesome --reporter-options reportDir=\"cypress/results\",overwrite=false,html=true,json=true && npx mochawesome-merge \"cypress/results/*.json\" > mochawesome.json && npx marge mochawesome.json"
  }
}
